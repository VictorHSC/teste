# Dockerfile for Frontend
FROM openjdk:17-oracle

# Define the working directory inside the container
WORKDIR /app

# Copy the Maven build file and source code
COPY . /app

# Package the frontend project (Maven command to build)
RUN ./mvnw clean package -DskipTests

# Expose the port for the frontend (e.g., 8080)
EXPOSE 8080

# Run the JAR file generated by Maven
CMD ["java", "-jar", "target/frontend.jar"]
